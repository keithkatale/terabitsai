/** Active PixelBlast speed — keep trace shimmer in sync. */
export const AGENT_PIXEL_ACTIVE_SPEED = 7.8;

/** Shimmer duration (seconds) — balanced speed for readability. */
export const TRACE_SHIMMER_DURATION_S = 0.9;
